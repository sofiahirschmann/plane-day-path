# OpenSky Network API credentials (OAuth2 client credentials).
# Create an API client at https://opensky-network.org → your account → "API Clients",
# then copy the id and secret here. See README.md for the full walkthrough.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
